subprojects {
    buildscript {
        repositories {
            jcenter()
        }
    }

    repositories {
        mavenLocal()
        jcenter()
    }

    group = GROUP
    version = VERSION_NAME
}